{
  "name": "hitsondrip-approver",
  "version": "0.1.0",
  "description": "Cloudflare Worker that handles Slack button approvals for the hitsondrip daily IG/X post pipeline. Verifies Slack signing secret, then calls Metricool to publish (approve) or delete (reject) the draft posts.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240502.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
